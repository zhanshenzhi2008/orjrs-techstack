# spring.config.name=eureka-server-cluster
# 端口
server.port=18762
# 注册中心路径，如果有多个eureka server，在这里需要配置其他eureka server的地址，用","进行区分
eureka.client.service-url.defaultZone=http://localhost:18761/eureka/,http://localhost:18763/eureka/